{
  "name": "ocwatch",
  "version": "0.1.0",
  "description": "Real-time web dashboard for monitoring OpenCode agent activity",
  "type": "module",
  "main": "src/server/index.ts",
  "scripts": {
    "dev": "bun run src/server/index.ts",
    "start": "bun run src/server/index.ts",
    "test": "bun test",
    "build": "bun build ./src/client/index.tsx --outdir ./dist --target browser"
  },
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
